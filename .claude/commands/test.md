Write tests for the selected code.

1. Prefer skill `flutter-add-widget-test` for UI; `dart-add-unit-test` for pure logic.
2. Use `mocktail` (not mockito).
3. Existing examples: `test/` (bloc_test for TripListBloc, OrderBloc; RouteService unit tests).
4. Run `flutter test` and fix failures.
