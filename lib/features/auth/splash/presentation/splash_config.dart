/// Splash screen timing and layout constants.
abstract final class SplashConfig {
  static const displayDuration = Duration(seconds: 2);

  static const entryAnimation = Duration(milliseconds: 700);

  /// Wordmark height when [AppBrandIcon.filled] is false.
  static const wordmarkHeight = 96.0;
}
